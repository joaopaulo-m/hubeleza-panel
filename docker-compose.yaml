version: '3.9'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: hubeleza-panel:1.0
    container_name: hubeleza-panel
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: production
    env_file:
      - .env
